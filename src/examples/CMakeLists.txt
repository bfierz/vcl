SUBDIRS(simd)

IF(CUDA_FOUND AND VCL_CUDA_SUPPORT)
	SUBDIRS(fluids)
ENDIF()

IF(CUDA_FOUND AND VCL_CUDA_SUPPORT AND VCL_OPENGL_SUPPORT)
	SUBDIRS(rtwturb)
ENDIF()

IF(VCL_OPENGL_SUPPORT)
	SUBDIRS(meshviewer)
	SUBDIRS(opengl)
ENDIF()

IF(OPENCL_FOUND AND VCL_OPENCL_SUPPORT)
	SUBDIRS(opencl)
ENDIF()
